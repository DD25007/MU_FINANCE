# LoRA-Credit-Unlearn dependencies
# PyTorch (install separately via conda/pip with CUDA support -- see setup instructions below)
# torch==2.5.1+cu121  -> installed via: pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu121

# Core ML
torch>=2.1.0
torchvision
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0
scikit-learn>=1.3.0

# Utilities
tqdm>=4.65.0
matplotlib>=3.7.0
seaborn

# Tabular DL / experiment tracking
rtdl
tab-transformer-pytorch
wandb
